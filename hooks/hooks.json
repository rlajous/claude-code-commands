{
  "description": "Git Workflow — optional background review of new commits and pushes (opt in via .git-workflow/git-workflow.local.md)",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}/hooks/review-commit.sh\" --host codex",
            "async": true,
            "timeout": 120,
            "statusMessage": "Checking whether the new commit needs review"
          }
        ]
      }
    ]
  }
}
